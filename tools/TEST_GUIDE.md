# 半自动内容生产流 - 详细测试指南

## 📋 测试前准备

### 1. 检查必需文件

确保以下文件存在：

```bash
cd tools
ls -la
```

**必需文件：**
- ✅ `step1_get_prompt.py` - Prompt 生成器
- ✅ `step2_save_chapter.py` - 入库工具
- ✅ `curriculum_manager.py` - 课程管理器
- ✅ `ielts_source.json` - 词源文件（至少需要一些单词）
- ✅ `story_config.json` - 故事配置文件
- ✅ `raw_story.txt` - 原始故事文件（用于粘贴 ChatGPT 生成的内容）
- ✅ `progress_tracker.json` - 进度追踪文件（如果不存在会自动创建）

### 2. 检查词源数据

```bash
# 检查 ielts_source.json 是否有词汇
cat ielts_source.json
```

如果没有词汇或文件不存在，需要先创建：

```bash
# 创建示例词源文件
cat > ielts_source.json << 'EOF'
[
  "ambitious", "meticulous", "prestigious", "sophisticated", "resilient",
  "elaborate", "profound", "intricate", "substantial", "comprehensive",
  "remarkable", "exceptional", "formidable", "influential", "strategic",
  "innovative", "transformative", "outstanding", "distinguished", "eminent"
]
EOF
```

### 3. 检查故事配置

```bash
cat story_config.json
```

如果没有，创建默认配置：

```bash
cat > story_config.json << 'EOF'
{
  "genre": "都市重生",
  "prev_summary": "",
  "chapter_outline": "主角重生回到过去，通过智慧和实力打脸，展现商业天赋，最终获得成功。"
}
EOF
```

---

## 🚀 完整测试流程

### **步骤 1：生成 Prompt（获取原材料）**

#### 1.1 运行 Prompt 生成器

```bash
cd "/Users/lindadong/Desktop/小创意/【破局】AI编程出海/IELTS Novel Flow/tools"
python3 step1_get_prompt.py
```

#### 1.2 预期输出

```
============================================================
📝 步骤1：智能选词与 Prompt 生成器
============================================================

📚 初始化课程管理器...
📊 学习进度统计
==================================================
总词汇量：XX 个
已学习：XX 个
待学习：XX 个
学习进度：XX%
当前章节：第 X 章
==================================================

📖 获取新单词批次（20个）...
新单词：['ambitious', 'meticulous', ...]

🔄 获取复习单词（5个）...
复习单词：['word1', 'word2', ...]

📝 加载故事配置...
流派：都市重生

✨ 生成 Prompt...
✅ Prompt 已保存到：tools/current_prompt.txt

============================================================
📋 生成的 Prompt（可直接复制到 ChatGPT）：
============================================================

[完整的 Prompt 内容会显示在这里]
```

#### 1.3 检查生成的文件

```bash
# 查看生成的 Prompt
cat current_prompt.txt
```

**关键检查点：**
- ✅ Prompt 中明确说明"1500字左右的完整短篇小说"
- ✅ 包含 20 个新单词列表
- ✅ 包含 5 个复习单词（如果有）
- ✅ 包含故事背景和流派信息

---

### **步骤 2：使用 ChatGPT 生成内容**

#### 2.1 复制 Prompt

打开 `tools/current_prompt.txt`，复制全部内容（包括 System Prompt 和 User Prompt）。

#### 2.2 发送到 ChatGPT

1. 打开 [ChatGPT 网页版](https://chat.openai.com)
2. 新建对话
3. 粘贴完整的 Prompt（包括 `=== System Prompt ===` 和 `=== User Prompt ===` 部分）
4. 等待 ChatGPT 生成内容

#### 2.3 复制生成的内容

1. 复制 ChatGPT 的完整回复（只复制小说内容，不要包含其他说明）
2. 确保内容包含 `{word|meaning}` 格式的标记

#### 2.4 粘贴到 raw_story.txt

```bash
# 编辑 raw_story.txt（可以使用任何文本编辑器）
nano raw_story.txt
# 或
code raw_story.txt  # VS Code
# 或直接用编辑器打开文件，粘贴内容
```

**重要：**
- 直接粘贴 ChatGPT 生成的内容
- 不要添加额外的说明文字
- 确保内容在 1400-1600 字左右

---

### **步骤 3：入库上架（加工生产和入库）**

#### 3.1 运行入库脚本

```bash
cd "/Users/lindadong/Desktop/小创意/【破局】AI编程出海/IELTS Novel Flow/tools"
python3 step2_save_chapter.py
```

#### 3.2 交互流程

脚本会引导你完成以下步骤：

**3.2.1 读取内容**
```
📖 读取原始故事内容...
✅ 已读取 2845 字符
```

**3.2.2 验证格式**
```
🔍 验证内容格式...
✅ 格式验证通过，发现 25 个单词标记
   示例单词：ambitious, meticulous, prestigious...
```

**3.2.3 验证字数**
```
📏 验证字数...
字符数：2845
估算字数：约 1422 字（中文）
✅ 字数符合要求（约 1422 字）
```

**如果字数不在范围：**
- 会显示警告
- 询问是否继续（输入 `y` 继续，`N` 取消）

**3.2.4 选择分类（类型）**
```
📚 选择书籍分类（类型）...
可选分类：
  1. 重生 (reborn)
  2. 悬疑 (suspense)
  3. 言情 (romance)
  4. 商战 (business)

请选择分类编号（1-4）：1
✅ 已选择分类：重生
```

**3.2.5 选择或创建书籍**

**情况 A：已有同分类书籍**
```
📖 选择或创建书籍...

找到 3 本同分类书籍：
  1. 顶级豪门的继承人 - 流年 (ID: book-001)
  2. 重生之商业女王 - 佚名 (ID: book-002)
  3. 豪门千金的逆袭 - 爽文 (ID: book-003)
  4. 创建新书籍

请选择书籍编号（1-4）：4
```

**情况 B：没有同分类书籍**
```
📖 选择或创建书籍...
当前分类下暂无书籍，创建新书籍：
请输入书名（直接回车使用自动提取）：重生豪门千金
使用自动提取的书名：重生豪门千金
请输入作者（可选，直接回车使用默认）：佚名
```

**如果选择"创建新书籍"：**
- 输入书名（或直接回车使用自动提取的标题）
- 输入作者（或直接回车使用默认"佚名"）

**3.2.6 处理数据**
```
📝 提取章节标题...
✅ 章节标题：重生豪门千金

📚 初始化课程管理器...
当前章节号：1

🔍 提取目标单词列表...
✅ 从 Prompt 文件提取到 20 个目标单词
   单词列表：['ambitious', 'meticulous', ...]

📦 构建章节数据...
💾 保存章节...
✅ 章节已保存：../src/data/generated/book-20240108123456.json

✅ 更新学习进度...
✅ 已标记 20 个单词为已学习

📊 更新后的学习进度：
==================================================
总词汇量：74 个
已学习：20 个
待学习：54 个
学习进度：27.0%
当前章节：第 2 章
==================================================

🧹 清理临时文件...
是否清空 raw_story.txt？(y/N): y
✅ 已清空 raw_story.txt
```

**3.2.7 完成提示**
```
============================================================
🎉 处理完成！
============================================================
📚 书籍信息：
   书名：重生豪门千金
   作者：佚名
   分类：重生
   书籍ID：book-20240108123456

📖 章节信息：
   章节标题：重生豪门千金
   内容长度：约 1422 字（2845 字符）
   保存位置：../src/data/generated/book-20240108123456.json

💡 提示：请检查 library.ts 是否已正确更新
============================================================
```

---

### **步骤 4：验证结果**

#### 4.1 检查生成的文件

```bash
# 检查章节文件是否生成
ls -la ../src/data/generated/book-*.json

# 查看生成的章节文件内容
cat ../src/data/generated/book-20240108123456.json | head -20
```

**预期格式：**
```json
{
  "id": "book-20240108123456-chapter-1",
  "title": "重生豪门千金",
  "content": "...",
  "chapter_num": 1,
  "book_id": "book-20240108123456"
}
```

#### 4.2 检查 library.ts 是否更新

```bash
# 查看 library.ts（应该在文件末尾看到新添加的书籍）
cat ../src/data/library.ts | tail -20
```

**预期格式：**
```typescript
  {
    id: "book-20240108123456",
    title: "重生豪门千金",
    author: "佚名",
    coverColor: "#8B5CF6",
    category: "reborn",
    chapters: [],
  },
```

#### 4.3 检查前端是否能读取

```bash
# 启动开发服务器（如果还没启动）
cd ..
npm run dev
```

然后访问：
1. `http://localhost:3000` → 登录
2. `http://localhost:3000/library` → 查看图书馆
3. 选择对应的分类（如"重生"）
4. 应该能看到新创建的书籍
5. 点击"开始阅读"按钮，应该能正常显示内容

---

## 🐛 常见问题排查

### 问题 1：step1 提示"没有可用的新单词"

**原因：** `ielts_source.json` 为空或 `progress_tracker.json` 中所有单词都已学习

**解决方案：**
```bash
# 检查词汇源
cat ielts_source.json

# 如果需要重置进度（删除已学习的标记）
rm progress_tracker.json
# 下次运行时会重新初始化
```

### 问题 2：step2 提示"格式验证失败"

**原因：** ChatGPT 生成的内容没有使用 `{word|meaning}` 格式

**解决方案：**
- 检查 `raw_story.txt` 中的内容
- 确保内容包含格式标记，例如：`她感到非常 {ambitious|有野心}`
- 如果没有，需要在 ChatGPT 中重新生成，并强调格式要求

### 问题 3：step2 提示"字数过多/过少"

**原因：** ChatGPT 生成的内容字数不在 1400-1600 字范围

**解决方案：**
- 可以在 ChatGPT 中要求重新生成，强调字数要求
- 或者手动编辑 `raw_story.txt` 调整字数
- 或者在提示中选择 `y` 继续（但建议重新生成）

### 问题 4：library.ts 没有自动更新

**原因：** 脚本无法正确解析 TypeScript 文件格式

**解决方案：**
- 脚本会显示手动添加的代码
- 复制显示的代码，手动添加到 `src/data/library.ts` 的 `libraryBooks` 数组中

### 问题 5：前端看不到新书籍

**原因：** `library.ts` 更新后，Next.js 需要重新编译

**解决方案：**
```bash
# 重启开发服务器
# Ctrl+C 停止服务器
npm run dev
```

---

## ✅ 测试检查清单

完成一次完整测试后，检查以下项目：

- [ ] `current_prompt.txt` 已生成，包含完整 Prompt
- [ ] `raw_story.txt` 已包含 ChatGPT 生成的内容
- [ ] `src/data/generated/book-*.json` 已生成章节文件
- [ ] `src/data/library.ts` 已更新（自动或手动）
- [ ] `progress_tracker.json` 已更新学习进度
- [ ] 前端能显示新书籍
- [ ] 前端能正常阅读新书籍内容
- [ ] 单词标记 `{word|meaning}` 能正确显示和点击

---

## 🎯 快速测试命令序列

如果你熟悉命令行，可以使用以下快速命令：

```bash
# 1. 生成 Prompt
cd tools && python3 step1_get_prompt.py

# 2. （手动）复制 Prompt 到 ChatGPT，生成内容，粘贴到 raw_story.txt

# 3. 入库上架
python3 step2_save_chapter.py

# 4. 检查结果
ls -la ../src/data/generated/book-*.json
cat ../src/data/library.ts | tail -10

# 5. 启动前端验证
cd .. && npm run dev
```

---

## 📝 测试日志模板

建议记录每次测试的信息，便于追踪问题：

```
测试日期：2024-01-08
测试编号：001

步骤1结果：
- 新单词数量：20
- 复习单词数量：5
- Prompt 长度：XXXX 字符

步骤2结果：
- 生成的字数：XXXX 字
- 选择的分类：重生
- 选择的书籍：创建新书 / book-001
- 章节文件：book-XXXX.json
- library.ts 更新：成功 / 失败（手动）

前端验证：
- 书籍显示：✅ / ❌
- 阅读功能：✅ / ❌
- 单词点击：✅ / ❌

遇到的问题：
[记录任何问题]
```

---

祝你测试顺利！如有问题，请检查上述常见问题排查部分。

